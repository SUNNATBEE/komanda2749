# Azamat — Toyota modellari

## Fayl

`src/pages/Azamat/ModelsPage.jsx`

## Figma

`figma/Azamat.jpg`, `Azamat-1.jpg`

## Ochish

**«Модели Toyota»** → `/models`

## Vazifa

Toyota rasprodaja hero, model grid, arxiv, maxsus takliflar, davlat dasturi, afzalliklar, sharhlar, blog.

Ichki bo'limlar uchun `src/pages/Azamat/components/` ishlating.

---

## GitHub — sizning branchingiz: `feature/azamat`

> To'liq qo'llanma: [`docs/GIT_GUIDE.uz.md`](../../GIT_GUIDE.uz.md)

```bash
git clone https://github.com/TASHKILOT/2749.git && cd 2749 && npm install
git checkout main && git pull origin main
git checkout -b feature/azamat
```

```bash
git add src/pages/Azamat/
git commit -m "feat(azamat): Toyota model qatori"
git push -u origin feature/azamat
```

| Buyruq | Vazifasi |
|--------|----------|
| `git checkout -b feature/azamat` | Branch ochish |
| `git add src/pages/Azamat/` | Faqat o'z fayllaringiz |
| `git commit -m "..."` | Saqlash |
| `git push` | GitHubga yuborish |
| `git pull origin main` | Jamoa yangilanishlarini olish |
| **PR orqali Merge** | `main` ga birlashtirish |
